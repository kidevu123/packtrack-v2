__pycache__/
*.pyc
.venv/
.env
*.db
.DS_Store
.ruff_cache/
.pytest_cache/
node_modules/
dist/
build/
*.egg-info/
uploads/
.superpowers/

# Tailwind build output — generated from static/styles.src.css on every
# deploy. Never check in the built CSS (avoids stale-asset confusion and
# review-noise diffs).
static/styles.css

# Local Python venvs used for running tests outside the main .venv.
.venv-test/

# Per-session Claude Code state. Kept untracked by convention; v2.16.1
# made this explicit so the deploy guard's dirty-tree check does not
# fail when a session directory is present in the working tree.
.claude/
